/* Literals

//JS defines two types of values:
    1: Fixed values are caleld literals
    2: Variable values are called variables

    Literals include: Arrays, numbers, booleans, objects, strings
*/

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString());

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley'
};

console.log(soup.b);

let car = 'Honda';

let decimal = 10.2;


